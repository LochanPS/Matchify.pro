// PaymentSummary is no longer used — fee breakdown is inline in TournamentRegistrationPage step 1.
// Kept as empty export to avoid breaking any stale imports.
export default function PaymentSummary() { return null; }
